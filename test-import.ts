// Test file to verify the package works correctly with updated features
import { Chatbot } from './dist/esm';

// This is just a basic type check to ensure the package exports correctly
console.log('Chatbot component:', Chatbot);
console.log('Successfully tested updated features: different bot/user icon backgrounds and improved icon visibility');